#include <stdio.h>

int main() {
    int num1, num2, sum;

    printf("Enter 3 integers: ");
    scanf("%d %d", &num1, &num2);

    sum = num1 + num2 + num3;

    printf("The sum is: %d\n", sum);

    return 0;
}
